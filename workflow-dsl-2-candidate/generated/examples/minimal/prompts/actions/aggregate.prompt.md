# Aggregate and Route Findings

Consume the admitted findings of all isolated review lenses behind the barrier. Preserve provenance, merge only clear common causes, and detect contradictions.

Return a structured routing result with `routing` in {`finalize`, `re-review`, `fail`}. Never vote, never change severity without the source lens, and never close a finding yourself.
