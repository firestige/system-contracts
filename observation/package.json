{
  "name": "@workflow-self-recursive/observation-contract",
  "version": "0.3.0",
  "private": true,
  "description": "Machine schemas, registry, fixtures, and validator for the MVP Observation contract candidate.",
  "scripts": {
    "build:publication": "node tools/build-publication-record.cjs",
    "test": "node --test tools/test-*.cjs",
    "check": "node tools/check-corpus.cjs"
  },
  "dependencies": {
    "ajv": "8.20.0"
  }
}
