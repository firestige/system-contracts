name: Contract publication candidate

on:
  push:
    branches:
      - release/next
  workflow_dispatch:
    inputs:
      candidate_tag:
        required: true
        type: string
      authority_ref:
        description: Exact superproject semantic-authority revision
        required: true
        type: string
      consumer_ref:
        description: Exact workflow-package consumer revision
        required: true
        type: string
  workflow_call:
    inputs:
      candidate_tag:
        required: false
        type: string
      authority_ref:
        required: false
        type: string
      consumer_ref:
        required: false
        type: string

permissions:
  contents: write

jobs:
  qualify:
    runs-on: ubuntu-latest
    steps:
      - name: Verify release trigger branch
        run: test "$GITHUB_REF_NAME" = "release/next"
      - name: Resolve immutable release request
        id: request
        env:
          GH_TOKEN: ${{ github.token }}
          DISPATCH_CANDIDATE_TAG: ${{ inputs.candidate_tag }}
          DISPATCH_AUTHORITY_REF: ${{ inputs.authority_ref }}
          DISPATCH_CONSUMER_REF: ${{ inputs.consumer_ref }}
        run: |
          if test "$GITHUB_EVENT_NAME" = "push" || test -z "$DISPATCH_CANDIDATE_TAG"; then
            gh api "repos/$GITHUB_REPOSITORY/contents/release/request.json?ref=$GITHUB_SHA" \
              --jq .content | base64 --decode > "$RUNNER_TEMP/release-request.json"
            CANDIDATE_TAG="$(jq -er .candidate_tag "$RUNNER_TEMP/release-request.json")"
            AUTHORITY_REF="$(jq -er .authority_ref "$RUNNER_TEMP/release-request.json")"
            CONSUMER_REF="$(jq -er .consumer_ref "$RUNNER_TEMP/release-request.json")"
          else
            CANDIDATE_TAG="$DISPATCH_CANDIDATE_TAG"
            AUTHORITY_REF="$DISPATCH_AUTHORITY_REF"
            CONSUMER_REF="$DISPATCH_CONSUMER_REF"
          fi
          test -n "$CANDIDATE_TAG" && test -n "$AUTHORITY_REF" && test -n "$CONSUMER_REF"
          echo "candidate_tag=$CANDIDATE_TAG" >> "$GITHUB_OUTPUT"
          echo "authority_ref=$AUTHORITY_REF" >> "$GITHUB_OUTPUT"
          echo "consumer_ref=$CONSUMER_REF" >> "$GITHUB_OUTPUT"
      - name: Check out semantic authority
        uses: actions/checkout@v6
        with:
          repository: firestige/workflow-self-recursive
          ref: ${{ steps.request.outputs.authority_ref }}
      - name: Check out contracts under qualification
        uses: actions/checkout@v6
        with:
          path: system-contracts
          fetch-depth: 0
      - name: Check out frozen consumer binding
        uses: actions/checkout@v6
        with:
          repository: firestige/workflow-package
          ref: ${{ steps.request.outputs.consumer_ref }}
          path: workflow-package
      - uses: actions/setup-node@v7
        with:
          node-version: 24.12.0
      - name: Run acceptance
        run: |
          npm --prefix system-contracts/workflow-dsl ci && npm --prefix system-contracts/workflow-dsl test
          npm --prefix system-contracts/observation ci && npm --prefix system-contracts/observation test && npm --prefix system-contracts/observation run check
          npm --prefix system-contracts/evaluation ci && npm --prefix system-contracts/evaluation test && npm --prefix system-contracts/evaluation run check:example
          npm --prefix system-contracts/delivery-admission test
          node --test system-contracts/test/tooling/release/release.test.cjs
      - name: Build and verify publication assets
        run: |
          node system-contracts/release/cli/release.cjs build "$RUNNER_TEMP/local-release" "$GITHUB_SHA"
          node system-contracts/release/cli/release.cjs verify "$RUNNER_TEMP/local-release"
      - name: Resolve resumable RC state
        id: candidate
        env:
          GH_TOKEN: ${{ github.token }}
          CANDIDATE_TAG: ${{ steps.request.outputs.candidate_tag }}
        run: |
          if gh release view "$CANDIDATE_TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            test "$(gh release view "$CANDIDATE_TAG" --repo "$GITHUB_REPOSITORY" --json isPrerelease --jq .isPrerelease)" = true
            test "$(gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$CANDIDATE_TAG" --jq .object.sha)" = "$GITHUB_SHA"
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            if git -C system-contracts ls-remote --exit-code --tags origin "refs/tags/$CANDIDATE_TAG" >/dev/null 2>&1; then
              test "$(gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$CANDIDATE_TAG" --jq .object.sha)" = "$GITHUB_SHA"
            fi
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Create or resume RC with exact local assets
        env:
          GH_TOKEN: ${{ github.token }}
          CANDIDATE_TAG: ${{ steps.request.outputs.candidate_tag }}
          RC_EXISTS: ${{ steps.candidate.outputs.exists }}
        run: |
          if test "$RC_EXISTS" != true; then
            gh release create "$CANDIDATE_TAG" --repo "$GITHUB_REPOSITORY" --prerelease --target "$GITHUB_SHA"
          fi
          mkdir "$RUNNER_TEMP/existing-assets"
          for FILE in "$RUNNER_TEMP/local-release/"*; do
            NAME="$(basename "$FILE")"
            if gh release view "$CANDIDATE_TAG" --repo "$GITHUB_REPOSITORY" --json assets --jq '.assets[].name' | grep -Fxq "$NAME"; then
              gh release download "$CANDIDATE_TAG" --repo "$GITHUB_REPOSITORY" --pattern "$NAME" --dir "$RUNNER_TEMP/existing-assets"
              cmp "$FILE" "$RUNNER_TEMP/existing-assets/$NAME"
            else
              gh release upload "$CANDIDATE_TAG" "$FILE" --repo "$GITHUB_REPOSITORY"
            fi
          done
          mkdir "$RUNNER_TEMP/remote-release"
          gh release download "$CANDIDATE_TAG" --repo "$GITHUB_REPOSITORY" --dir "$RUNNER_TEMP/remote-release"
          node system-contracts/release/cli/release.cjs verify "$RUNNER_TEMP/remote-release"
          cmp "$RUNNER_TEMP/local-release/release-metadata.json" "$RUNNER_TEMP/remote-release/release-metadata.json"
      - name: Attach qualification evidence
        env:
          GH_TOKEN: ${{ github.token }}
          CANDIDATE_TAG: ${{ steps.request.outputs.candidate_tag }}
        run: |
          export CANDIDATE_TAG
          node -e 'const fs=require("node:fs"),crypto=require("node:crypto"),path=require("node:path"); const p=process.env.RUNNER_TEMP,m=fs.readFileSync(path.join(p,"remote-release/release-metadata.json")); fs.writeFileSync(path.join(p,"release-qualification.json"),JSON.stringify({schemaVersion:"wsr.release-qualification@1.0.0",candidateTag:process.env.CANDIDATE_TAG,commit:process.env.GITHUB_SHA,artifactMetadataSha256:"sha256:"+crypto.createHash("sha256").update(m).digest("hex"),localAcceptance:{status:"PASS"},remoteQualification:{status:"PASS"}},null,2)+"\n")'
          if gh release view "$CANDIDATE_TAG" --repo "$GITHUB_REPOSITORY" --json assets --jq '.assets[].name' | grep -Fxq release-qualification.json; then
            mkdir "$RUNNER_TEMP/existing-qualification"
            gh release download "$CANDIDATE_TAG" --repo "$GITHUB_REPOSITORY" --pattern release-qualification.json --dir "$RUNNER_TEMP/existing-qualification"
            cmp "$RUNNER_TEMP/release-qualification.json" "$RUNNER_TEMP/existing-qualification/release-qualification.json"
          else
            gh release upload "$CANDIDATE_TAG" "$RUNNER_TEMP/release-qualification.json" --repo "$GITHUB_REPOSITORY"
          fi
