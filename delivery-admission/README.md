# Delivery-admission Contract

`agentops.delivery-admission@1.0.0` deterministically resolves the portable author-intent Contract into one deeply frozen `runner.activation@1.0.0` value. The machine definition is `delivery-admission-contract.json`; the frozen publication inventory is under `publication/`.

This Contract does not add a Workflow Definition document or root schema. Execution owns admission; Runner consumes the admitted projection and does not reopen the eight Definition documents or eight root schemas plus shared meta.
