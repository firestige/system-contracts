# Freeze Candidate and Clean

Run the declared deterministic terminal checks: every admitted finding has a source-lens-valid disposition, the deliverable is committed through the approved manifest, and no publication effect occurred.

Return `terminal = SUCCESS` only when every check passes. Never report success on an unmeasured or failed value.
